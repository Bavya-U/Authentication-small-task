export const environment = {
    production: false,
    serverURL: "http://localhost:8080",
  };
  // const token = localStorage.getItem("token");
  
  export const headers = {
    "content-Type": "application/json",
    accept: "application/json",
    // authorization: `logiKey ${token}`,
  };
  